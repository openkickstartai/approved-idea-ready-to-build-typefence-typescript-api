{
  "name": "typefence",
  "version": "0.1.0",
  "description": "TypeScript API boundary runtime guard generator — eliminate type illusions",
  "main": "dist/index.js",
  "bin": {
    "typefence": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": ["<rootDir>/test"]
  },
  "keywords": ["typescript", "zod", "runtime-validation", "codegen", "cli", "api-safety"],
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
